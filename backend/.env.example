# Local dev works with no DATABASE_URL set (falls back to SQLite in backend/app/dev.db).
# For Supabase Postgres, get this from Project Settings > Database > Connection string (URI, "Session pooler")
# and swap postgres:// for postgresql://
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Comma-separated origins allowed to call this API
FRONTEND_ORIGIN=http://localhost:3000

# Must match AUTH_SECRET in frontend/.env.local exactly — verifies the
# per-user JWTs Next.js mints after Google sign-in.
AUTH_SECRET=

# Must match INTERNAL_API_SECRET in frontend/.env.local — only used for the
# server-to-server user-sync call during sign-in.
INTERNAL_API_SECRET=
